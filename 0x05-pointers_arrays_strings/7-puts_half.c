#include "main.h"
/**
 *puts_half - printing second half of string
 *@str: string to check
 *
 */
void puts_half(char *str)
{
	int i;
	
	while (str[i] != '\0')
	{
		i++
		if (i % 2 -- 0)

		
		_putchar(str[i]);
		i++;
	}
}
